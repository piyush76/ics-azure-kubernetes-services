apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ics-dev-msds-nfs-pv-claim
spec:
  storageClassName: persistent-nfs-storage
  volumeName: ics-dev-msds-nfs-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ics-dev-receiptimages-nfs-pv-claim
spec:
  storageClassName: persistent-nfs-storage
  volumeName: ics-dev-receiptimages-nfs-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ics-dev-docimages-nfs-pv-claim
spec:
  storageClassName: persistent-nfs-storage
  volumeName: ics-dev-docimages-nfs-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
